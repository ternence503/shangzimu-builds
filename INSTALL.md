# 上字幕：安裝與使用

安裝包尚未發布。以下是待發布測試包的指引，發布時仍須核對檔名與實際操作。不要用原始碼 ZIP 代替安裝包。

## Mac

1. Apple 選單 →「關於這台 Mac」：Apple M 系列選 Apple 晶片版，Intel 選 Intel 版。
2. 從本 repo Releases 下載對應 `.pkg`，核對版本與公布的 SHA256。
3. 執行安裝程式，依提示安裝至「應用程式」。
4. 在「應用程式」開啟「上字幕」，或用 Spotlight 搜尋「上字幕」。搜尋索引不一定立即完成。

本版未正式簽署、公證。若因不明開發者阻擋，先核對來源，再依 [Apple 官方說明](https://support.apple.com/en-my/102445) 到「系統設定 → 隱私權與安全性」查看「仍要打開／強制打開」。不要全域關閉保護；若警告含惡意軟體或已損壞，停止，不強行繞過。

## Windows

1. 從 Releases 下載 Windows x64 `.exe`，核對版本與 SHA256。
2. 執行安裝程式；預設安裝至個人使用者的應用程式資料位置，非 Program Files。
3. 從開始功能表開啟或搜尋「上字幕」。

本版未正式簽署，可能出現來源不明提示。若防毒或組織政策攔截，停止並回報，不關閉防毒或公司安全政策。

## 產生與整理字幕

1. 在「語音轉字幕」選擇音檔／影片，保留細部時間選項，開始轉錄。
2. 完成後點「整理字幕」，選模式與字數／行數，再點「自動切句與換行」。
3. 選取字幕修改文字／換行，必要時切分或與下一句合併。
4. 查看需要確認的字幕，逐段預覽校對。
5. 點「儲存進度，下次繼續」保存 JSON；點「匯出給剪輯軟體（SRT）」輸出字幕。SRT 不等於完整專案備份。
6. 在 Final Cut Pro／剪映匯入 SRT，檢查實際字型、位置及軟體自動換行。

## 常見問題

- **繼續工作：**「字幕排版 → 匯入 SRT／字幕專案」選之前保存的 JSON。「查看已儲存的檔案」開啟目前輸出資料夾。
- **只有 SRT：** 不一定保有細部時間，無法保證依語音精準切句；優先使用 JSON 或新轉錄的細部時間原稿。
- **只有 small：** 本測試版只封裝 small，不是付費／試用額度限制。
- **要求安裝 Python 或下載模型：** 不符合完整封裝目標，停止並回報版本及錯誤，不改跑舊啟動脚本。
- **回報問題：** 提供系統、晶片／處理器、版本、操作步驟與錯誤。先遮蔽私人路徑及內部資料，不公開上傳同事影音。
