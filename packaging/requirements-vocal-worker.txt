# Separate frozen CPU worker, never installed on the user's system Python.
# Install torch/torchaudio from https://download.pytorch.org/whl/cpu first.
torch==2.2.2
torchaudio==2.2.2
numpy==1.26.4
openunmix==1.3.0
PyInstaller==6.22.0
