# Developer build dependencies, never installed onto the end user's machine.
# Each native build records its actual resolution in dependency-lock.txt.
PyInstaller==6.22.0
faster-whisper==1.2.1
av==18.1.0
ctranslate2==4.8.2
opencc-python-reimplemented==0.1.7
edge-tts==7.2.7
